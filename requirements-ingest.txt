# Corpus ingestion only — run locally, never bundled into the Vercel function.
#   pip install -r requirements.txt -r requirements-ingest.txt
#   python3 -m api.ingest
-r requirements.txt
requests>=2.32,<3
beautifulsoup4>=4.12,<5
langchain-text-splitters>=0.3,<0.4
